# 《增刪卜易》Stage 1.6 独立 Skill 晋级评审

> 目标：用最少的可发现入口覆盖 10 个 verified canonical，同时避免与已安装的 `traditional-divination-skill` 产生重复触发。`router` 表示保留完整能力卡，不表示淘汰。

| canonical | 独立意图 | 独立契约 | 独立运行 | 独立复用 | 独立评测 | 去向 | 理由 |
|---|---:|---:|---:|---:|---:|---|---|
| VC-01 来源分层 | 否 | 是 | 是 | 是 | 是 | router | 用户通常在查本书版本时触发，属于来源入口职责。 |
| VC-02 一事一问 | 否 | 是 | 是 | 是 | 是 | router | 是占问前置步骤，不应另建一个与主占问入口竞争的 Skill。 |
| VC-03 六次投掷编码 | 否 | 是 | 是 | 是 | 是 | router | 可独立计算，但基础 Skill 已负责起卦；书本包只补来源解释。 |
| VC-04 查表与局部变换 | 否 | 是 | 否 | 是 | 是 | router | 需要已建立的卦、日期与字段上下文，单独触发容易误用。 |
| VC-05 用神关系链 | 否 | 是 | 否 | 是 | 是 | router | 依赖问题合同和排盘字段，且两现/不现仍有缺口。 |
| VC-06 四来源作用账本 | 否 | 是 | 否 | 是 | 是 | router | 是解释链内部证据账本，不能脱离上游字段运行。 |
| VC-07 规则冲突审计 | 否 | 是 | 是 | 是 | 是 | router | 可独立做文本审计，但主要用于本书规则校核，来源路由更准确。 |
| VC-08 应期候选生成 | 否 | 是 | 否 | 否 | 是 | router | 仅研究模式且缺排序与失败定义，不适合独立入口。 |
| VC-09 领域专章路由 | 否 | 是 | 否 | 是 | 是 | router | 属于来源入口的内部路由，不是用户独立任务。 |
| VC-10 高风险停止门 | 否 | 是 | 是 | 是 | 是 | router | 必须普遍执行，但应内嵌于主 Skill，不建立“高风险占断”入口。 |

## 预算与触发结论

- 可发现入口：`zengshan-buyi` 1 个；晋级独立 Skill 0 个；总数 1，低于软预算 8。
- 所有 10 个 active 能力由 router 唯一服务，写入 `destinations.json`。
- 以后如有真实路由记录或用户明确要求某项单独触发，再按 split-on-evidence 复审；默认不收集或上报使用数据。
